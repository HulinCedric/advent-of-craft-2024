using Microsoft.AspNetCore.Authentication;

namespace Reindeer.Web.Security;

public class ReindeerApiKeyAuthenticationOptions : AuthenticationSchemeOptions
{
    // Here you could add properties like RequiredHeaderName or so if needed
}